import React, { useState, useEffect } from 'react';
export const MapMessages = ({ messages, sc, s }) => {
    let m;
    if (!messages) {
        m = []
    } else {
        m = messages;
    }

    useEffect(() => { }, []);
    return (<>

    </>);
};